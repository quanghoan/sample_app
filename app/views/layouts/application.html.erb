<!DOCTYPE html>
<html>
  <head>
    <title><%= full_title(yield(:title)) %></title>
    <%= stylesheet_link_tag 'application', media: 'all',
                                           'data-turbolinks-track' => true %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
    <%= csrf_meta_tags %>
    <%= favicon_link_tag 'hoandz.jpg' %>
    <%= render 'layouts/shim' %>
    <meta charset="UTF-8">
    <meta name="description" content="quanghoan là trang web mang lại tiếng cười cho mọi người, bằng những hình ảnh vui nhộn và tiêu đề thú vị. Trang web dành riêng cho dân công sở và văn phòng.">
    <meta name="keywords" content="quanghoan, anh vui cong so, chuyen cuoi cong so, van phong vui, cong so vui">
  </head>
  <body>
    <%= render 'layouts/header' %>
    <div class="container">
      <% flash.each do |message_type, message| %>
        <div class="alert alert-<%=message_type %>"><%=message %></div>
      <% end %>
      <%= yield %>
      <%= render 'layouts/footer' %>
      <%# debug(params) if Rails.env.development? %>
    </div>
    
  </body>
</html>